/*3-Escreva uma função chamada verificarPar que recebe um número como parâmetro e retorna verdadeiro se o número for par e falso caso 
contrário.*/
function verificarPar(a) {
    var numero = 2;

    if (numero % 2 == 0) {
        console.log("esse numéro não Impar ele é Verdadeiro")
    } else {
        console.log("esse numéro não é par ele é Falso")
    }
}
verificarPar();